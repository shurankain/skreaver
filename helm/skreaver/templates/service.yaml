apiVersion: v1
kind: Service
metadata:
  name: {{ include "skreaver.fullname" . }}
  labels:
    {{- include "skreaver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skreaver.selectorLabels" . | nindent 4 }}
